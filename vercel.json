{
    "version": 2,
    "builds": [
        {
            "src": "./api/server.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        { "src": "./api/send", "methods": ["POST"], "dest": "./api/server.js" },
        { "src": "/(.*)", "dest": "/index.html" }
    ]
}